# Artemis Mobile App Environment Variables
#
# Copy this file to .env and update the values as needed.
# The .env file is ignored by git for security.

# API Base URL
#
# For local development with Expo Go on a physical device:
# - Start the tunnel: pnpm dev:tunnel (from project root)
# - The script will automatically update this value
#
# For simulators/emulators (no tunnel needed):
# - Leave this commented out or empty
# - The app will automatically use localhost (iOS) or 10.0.2.2 (Android)
#
# For production/EAS builds:
# - Set to your production API URL
#
# EXPO_PUBLIC_API_URL=https://artemis-dev.loca.lt

# Artemis API Server Environment Variables
#
# Copy this file to .env and update the values.
# The .env file is ignored by git for security.

# Server Configuration
PORT=4000

# PostgreSQL Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=artemis

# JWT Authentication
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# Google OAuth 2.0
#
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create or select a project
# 3. Create OAuth 2.0 Client ID credentials (Web application type)
# 4. Copy the Client ID and Client Secret here
#
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Google OAuth Callback URL
#
# For local development with tunnel (Expo Go on physical device):
# - Run: pnpm dev:tunnel (from project root)
# - Use the tunnel URL: https://artemis-dev.loca.lt/auth/google/callback
# - Add this URL to "Authorized redirect URIs" in Google Cloud Console
#
# For simulators only (no tunnel):
# - Use: http://localhost:4000/auth/google/callback
#
# IMPORTANT: The callback URL here MUST match what's configured in Google Cloud Console
#
GOOGLE_CALLBACK_URL=http://localhost:4000/auth/google/callback

# Default mobile redirect URI (optional)
# This is the deep link scheme used by the mobile app
# Default: artemis://auth
# MOBILE_REDIRECT_URI=artemis://auth